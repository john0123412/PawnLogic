# PawnLogic 0.3.6 Live Turn Control Plan

> **Status:** Active. Implementation and release evidence will be recorded here
> before the plan is marked complete.
>
> **Version note:** This cycle is 0.3.6 per the AGENT.md Version Numbering
> Policy: the minor (second) digit never moves without an explicit owner
> decision, so the live-turn-control scope ships as a patch release.

## Goal

Give the REPL mainstream live turn control: input stays available while a Turn
runs, mid-turn Enter steers at the next safe point, follow-up messages queue
for natural completion, and interruption becomes per-Turn and side-effect safe.
Today the REPL reads no input during a Turn; Ctrl+C is the only input path and
the MessageQueue mixes future work, pending work, and Ctrl+C retry in one
deque, silently drops messages when full, and shares one process-level
interrupt event.

## Interaction Contract

| Scenario | Behavior |
|---|---|
| Idle + Enter | Start a new Turn immediately. |
| Running + Enter | Submit a steer; it takes effect at the next safe point, or rolls forward as the next Turn if a text-only response completes first. |
| Running + Alt+Enter | Submit a follow-up; it runs after the current Turn finishes naturally. |
| Running + Esc | Interrupt the current Turn off the UI thread; after cancellation settles, prefill one editable recovered draft without auto-running it. |
| Alt+Up | Recall the most recent queued message into the editor. |
| /queue | Show steer, follow-up, and recovered entries without pausing the persistent terminal or active Turn. |
| /queue remove \<id\> | Remove one queued message. |
| /queue clear | Clear queued messages only. |
| /abort | Interrupt the active Turn only (semantics change; docs migrate in the same cycle). |
| /abort --all | Interrupt the active Turn and clear all queues. |
| Restart / resume | Restore conversation and queues; an unfinished Turn becomes an editable draft. Side-effect Tools are never replayed automatically. |

Steer safe point: after the current Tool Call completes. Tool calls from the
same batch that have not started yet are recorded as structured
`skipped` results so the protocol stays complete; their plan-guard accounting
follows ADR 0009. The steer is appended as a new user message and the model is
requested again. A streaming text-only response is not torn apart; if no safe
point occurs, each unclaimed steer remains independent and drains as a
subsequent Turn. Esc remains the immediate-interrupt path.

Guarantees: context, queued input, and completed tool results are never lost,
and side-effect tools are never replayed without confirmation. No guarantee of
resuming from the middle of an arbitrary syscall or network request.

## Phases

### P0 - Product Contract (this phase)

- This plan, ADR 0009, and the CONTEXT.md vocabulary below.
- A deterministic terminal E2E baseline for the live-composer contract. The
  Prompt Toolkit path must submit a steer while a real native blocking Tool
  Call is running, keep the model request count unchanged until that Tool Call
  completes, and apply the steer at the following safe point. Before the
  scheduler lands, only the precise missing-steer-context assertion is an
  imperative `pytest.xfail`; startup, input-mode, Tool Call, and trace setup
  failures remain hard failures. Companion passing tests cover text-only
  stream integrity and the serial readline fallback.

Gate: owner reviews the contract and ADR before any P1 implementation.

### P1 - TurnScheduler

- New deep module `core/turn_scheduler.py` replacing (not wrapping)
  `core/message_queue.py`.
- Three-entry interface: `submit(submission)`, `control(action)`,
  `view()`. Stable submission IDs, FIFO within each lane, and an explicit
  error instead of silent drop when full. Mixed lanes follow the safe-point
  policy: steer takes precedence over follow-up at P3; P1 only admits steer
  and leaves it queued until that safe point. The unused `priority` field and
  the module-level singleton are removed.
- State machine: IDLE, RUNNING, STEER_QUEUED, FOLLOW_UP_QUEUED, RECOVERABLE.
- Production executor Adapter wraps `AgentSession._run_turn_active()`; tests
  use an in-memory Adapter. CLI, remote control, and tests share one Interface.
- Acceptance: pure state-machine tests with no sleeps (concurrency tests use
  Event/barrier); `core/session.py` net line count decreases (budget is
  2100 and current headroom is near zero).

### P2a - Always-available input

- One persistent full-screen Prompt Toolkit application owns the terminal.
  Its output viewport stays above a fixed bottom composer and toolbar while a
  Turn streams; stdout, stderr, and Output Sink traffic are appended to the
  application buffer instead of repainting the physical prompt.
- Ctrl+C is handled as a key binding that calls `control()` and triggers
  cooperative cancellation. Python signal handlers may not interrupt the
  worker thread; the per-Turn CancellationToken from the delegation runtime
  pattern is the only cancellation mechanism.
- Acceptance: typing during a long tool execution works with zero output
  corruption and bounded Ctrl+C latency.

### P2b - Execution in a worker thread

- Turn execution moves to a worker thread through the TurnExecutor Adapter.
- Before implementation, an audit lands for module-level globals
  (`_user_mode`, `_debug_mode`, `_dynamic_config`) and RuntimeContext legacy
  mirrors: each is either encapsulated behind RuntimeContext or documented as
  thread-confined.
- Interactive tools (pwn_debugger and similar) are verified not to compete
  with the composer for tty stdin.
- The readline fallback stays serial with an explicit notice that live input
  requires Prompt Toolkit.
- Gate: owner sees a live demo of typing during a long tool execution.

### P3 - Steer and follow-up

- The steering queue is checked at Tool-call safe points; remaining batch
  tools get `skipped` results.
- Follow-ups are consumed only after natural stop, one per Turn, so distinct
  intents never merge into one user message.

### P4 - Interruption redefinition

- Global interrupt event replaced by per-Turn cancellation tokens.
- Esc and Ctrl+C trigger the same Turn control. `/abort` interrupts the active
  Turn only; `/queue clear` handles queued messages; `/abort --all` does both.
- An interrupt fully closes unfinished Tool Call protocol pairs.
- README (English and Chinese), the recovery hint, and any 0.3.5 references to
  the old `/abort` semantics migrate in the same commit series.

### P5 - Persistence and restart recovery

- Scheduler snapshots carry a schema version; every enqueue, edit, remove,
  claim, finish, and interrupt writes an atomic checkpoint.
- Old `queue_state` migrates: queued becomes follow-up, pending becomes a
  recovered draft.
- Restart never auto-replays pending side-effect Tools. Add
  `pawn --continue` and `pawn resume <session>`.

### P6 - Queue TUI

- Stable sequence numbers, type, content summary, and status; recall, remove,
  clear, and type conversion.
- Bottom toolbar shows `Running · steer: N · follow-up: N`.
- Up to three queued/recovered entries appear as muted read-only rows directly
  above the fixed composer; additional entries are summarized by count.

Implementation evidence: `core/queue_tui.py` renders immutable scheduler views
and returns cancellable typed menu choices for adapters that can safely own
stdin; the persistent terminal keeps `/queue` in its output viewport and never
pauses the active Turn. Deterministic text output supports `remove`, `steer`,
`follow-up`, and `recall` by stable ID. Conversion preserves ID, sequence,
source, metadata, and lane ordering.
Prompt Toolkit Alt+Up recalls without consuming an entry, while active queue
inspection never claims worker stdin. `tests/test_queue_tui.py`, scheduler
conversion coverage, and live-repl binding tests cover these paths without
sleep-based synchronization.

### Phase status and evidence

Implementation for P0-P6 is complete. The release candidate is being validated
on `test/release-0.3.6`; the plan remains **Active** until
the 0.3.6 release is published, as required by `docs/plans/INDEX.md`.

| Phase | Status | Evidence |
|---|---|---|
| P0 | Complete | The Prompt Toolkit safe-point, continuous-input preview/drain, interruption recovery, and serial fallback baselines are included in the 26 passing Dynamic E2E tests. |
| P1-P5 | Complete | Scheduler, live input, safe-point, cancellation, runtime-context, and persistence targeted suites previously recorded 112 passing tests. |
| P6 | Complete | Queue TUI, stable-ID controls, conversion, recall, toolbar, and active-stdin isolation targeted suites recorded 171 passing tests. |

Current combined validation evidence is 1,469 non-E2E tests passed (5 Python
tarfile warnings) and 26 Dynamic E2E tests passed. Ruff, typed-island mypy,
documentation structure, language policy, release consistency, architecture
budget, code-index, diff, package build, twine metadata, and isolated wheel
fresh-install checks pass. `core/provider_runtime.py` now satisfies its 400-line
and complexity budgets after model helpers moved to `core/provider_models.py`.
PR #120 passes the Python 3.10/3.11/3.12 matrix and Dynamic E2E in remote
Actions. Owner manual terminal acceptance remains before release finalization.

### Deferred

- Cross-process control (`pawn queue --session ...`) requires a Unix socket /
  app-server layer and is deferred past 0.3.6.

## Acceptance Gates

- Input and editing work during long tool executions; output stays aligned.
- The composer remains the penultimate terminal row and the toolbar remains
  the final row during rapid streaming; accepting steer/follow-up input never
  inserts that text into the model's current output line.
- Mouse-wheel events scroll only the output viewport, never composer history;
  manual history position survives new output until the user returns to the
  tail, which restores automatic following.
- Esc/Ctrl+C cancellation never blocks the Prompt Toolkit thread. After the
  Turn settles, editing the automatically prefilled recovered draft replaces
  and runs that prompt exactly once without first replaying the original.
- Consecutive Enter submissions remain visible as muted queue previews and
  drain independently after a text-only response; a stale keypress lane is
  reconciled against the latest scheduler snapshot instead of surfacing an
  internal error.
- Streamed output and its cursor metadata use one render snapshot, so a
  producer append cannot move the cursor past Prompt Toolkit's cached fragment
  lines or consume the next Enter in its exception screen.
- Steer takes effect at a safe point; follow-ups run only after natural
  completion; multiple messages stay independent, unmerged, unduplicated, and
  never silently dropped.
- After Esc the composer, queues, and context recover; repeated
  interrupt/retry cycles never replay tool side effects.
- Forced process exit restores queues without auto-replaying the active Tool.
- Pure state-machine tests do not sleep; concurrency tests use Event/barrier.
- Real terminal E2E on Linux/WSL2 and at least one real provider smoke pass.
- Ruff, typed-island mypy, documentation structure, language policy, release
  consistency, architecture budget, and code index pass; the Python
  3.10/3.11/3.12 matrix, package build, fresh-install smoke, Dynamic E2E, and
  remote Actions pass before publish.
- `/abort` documentation shows no mixed old/new semantics.

## Non-Goals

- No Provider behavior or retry-policy changes.
- No changes to the delegation runtime concurrency model.
- No plan-guard semantic changes beyond the steer accounting interface.
- No cross-process control in this cycle.

## Evidence

- Baseline: `main` at 67aae65 (v0.3.5 tagged and published).
- Working implementation merged locally into `main`; release validation
  continues on `test/release-0.3.6`. Version remains
  declared as unreleased 0.3.6 through the fixed-location version files.
- RED evidence: `tests/test_e2e.py::test_mid_turn_tool_steer_applies_at_tool_safe_point`
  runs the Prompt Toolkit path with a native blocking `read_file` Tool Call,
  submits a steer while the Tool Call is blocked, and releases it before
  checking the next model request. The pre-scheduler behavior is identified
  precisely when that request still contains only the original user message;
  only that mismatch invokes imperative `pytest.xfail`. The same file also
  verifies that a text-only stream is not torn apart, consecutive Enter input
  is previewed and drained, post-drain input remains usable, and the readline
  fallback remains serial.
