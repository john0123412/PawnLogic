# PawnLogic 0.3.7 Inline Terminal Stability Plan

> **Status:** Active; implementation is merged to `main` and release remains
> paused. The single-Application modal and live host-scrollback contracts
> have local and remote GREEN evidence, and owner PTY acceptance is complete.
> Tag and publish remain intentionally open.
>
> **Version note:** This cycle is 0.3.7 per the AGENT.md Version
> Numbering Policy: the minor (second) digit never moves without an
> explicit owner decision, so the inline-terminal and selector-modal
> scope ships as a patch release.

## Goal

Stabilize the live terminal by restoring native terminal scrollback,
mouse selection, and copy-paste, and unify the interactive selector
experience under a single Prompt Toolkit `Application` dialog state.
The 0.3.6 persistent terminal entered the alternate screen buffer and
owned the mouse, so the user's native terminal could not scroll, select,
or copy the live transcript. Its `/model`, `/planguard`, `/provider`, and
`/skills` selectors relied on a fragile "exit main `Application` ->
restore stdout -> run selector -> rebuild main `Application`" handoff
that breaks the persistent transcript, the queue preview, and the
toolbar.

## Scope

- Run the live composer without `full_screen=True` so the user's
  terminal keeps its own scrollback and native mouse selection. The
  persistent transcript moves to a `TerminalTranscript` buffer that
  flushes to the host terminal output through the same `Output` sink
  the readline fallback already uses.
- Run the live composer without `mouse_support=True` so wheel events
  flow to the host terminal first; coordinate-aware terminal
  capabilities are still routed to the output viewport when the
  terminal advertises them.
- Replace `LiveTerminalController.pause_for_modal` and its
  `resume_after_modal` pair with an in-`Application` dialog state. The
  same modal framework hosts `/model`, `/planguard`, `/provider`,
  `/skills`, and any future interactive selector; the main
  `Application` never tears down.
- Record the new constraints in
  [ADR 0010](../adr/0010-inline-terminal-modal.md).
- Keep the readline fallback serial and unchanged: the in-`Application`
  dialog state is gated on Prompt Toolkit being available and active.

## Acceptance Gates

- A PTY smoke test proves a long transcript is selectable with the host
  terminal's mouse and scrollable with the host terminal's wheel after
  the live composer is up.
- A PTY smoke test proves the output viewport still receives
  coordinate-aware scroll events when the terminal advertises them,
  while the host terminal keeps the default for everything else.
- An integration test proves `/model` opens the selector without exiting
  the main `Application`, selecting a model returns control to the live
  composer, and the queue preview, the toolbar, and the
  recovered-draft marker all survive the round trip.
- The same modal framework is reused by `/planguard`, `/provider`, and
  `/skills`; each one round-trips without rebuilding the main
  `Application`.
- Live composition key bindings (`Enter`, `Alt+Enter`, `Esc`, `Ctrl+C`,
  `Ctrl+Z`, `Alt+Up`) still behave exactly as documented in the 0.3.6
  plan.
- Readline fallback remains serial; the in-`Application` modal path is
  not reached when Prompt Toolkit is unavailable or
  `PROMPT_TOOLKIT_ENABLED=0`.
- Relevant tests, E2E, ruff, typed-island mypy, documentation checks,
  release consistency, architecture budget, code-index freshness, and
  `git diff --check` pass.
- A release candidate later passes the Python 3.10/3.11/3.12 matrix,
  package build, fresh-install smoke, Dynamic E2E, and remote Actions
  before publish.

## Explicit Non-Goals

- No version bump, tag, PyPI upload, GitHub Release, or remote branch
  operation in the implementation step.
- No additional change to the repaired Enter / Alt+Enter / Esc / queue /
  recovery contract already present on this branch.
- No change to the provider runtime, the tool registry, or the
  delegation orchestrator; the inline-terminal work is strictly UI
  plumbing.
- No additional change to the readline fallback, `DANGEROUS_PATTERNS`,
  or Network / Trust policies.
- No cleanup or merge of stale local branches without separate
  authorization.

## Risks

- Prompt Toolkit and the host terminal share the same input stream;
  turning the alternate screen off may expose previously hidden PT key
  sequences to the host shell. The bounded escape-sequence window from
  0.3.6 and the new `TerminalTranscript` buffer are the mitigations;
  real-input PTY smoke tests must keep them covered.
- The in-`Application` dialog state must not block the live Turn or
  steal input from the composer. Modal lifecycle, focus restoration,
  and per-frame layout must be deterministic; the persistent TUI's
  existing tests are the regression baseline.
- The single-`Application` selector must tolerate selectors that
  long-jump to another selector (e.g. `/model` from inside the provider
  TUI); the modal stack must be explicit and bounded.

## Evidence

- Baseline: `main` at `b33951c` (0.3.6 published; PR #123 merged). The
  0.3.6 release-state doc cleanup commit `8cbb1d3` lands on
  `rebuild/inline-terminal-0.3.7` first, so the active-plan pointer
  references a post-0.3.6 baseline.
- The initial 0.3.7 implementation shipped in two development phases.
  A later audit found that live scrollback still flushed only at close
  and that `/provider` and `/skills` still created nested Applications;
  the repair merged by PR #124 closes those gaps before release resumes.

### Phase A — persistent terminal (committed)

- `docs(plan): define the 0.3.7 inline terminal contract` (`c337f4b`).
  Adds this plan file and ADR 0010 in Proposed state. ADR 0010
  remains **Proposed** until phase B is done; the Acceptance section
  of the ADR lists the conditions.
- `test(0.3.7): RED baseline for inline terminal and single-Application
  modal` (`2593447`). 10 contract tests; all 10 fail against
  `main` `@ b33951c` with clean assertion / AttributeError failures,
  proving they actually exercise the contract rather than vacuously
  passing.
- `feat(0.3.7): wire TerminalTranscript into PersistentTerminal and
  add run_selector` (`cea90cb`). Adds `pawnlogic/terminal_transcript`,
  rewires the persistent terminal to use a single TerminalTranscript
  (no more parallel `_output_chunks` deque), drops `full_screen=True`
  / `mouse_support=True`, makes `pause_for_modal` / `resume_after_modal`
  flag flips that do not exit the main Application, and adds
  `PersistentTerminalController.run_selector` so commands can run a
  selector as a background task on the live Application.
- `chore(0.3.7): register TerminalTranscript as a typed-island
  module` (`0d8a203`). Adds `pawnlogic/terminal_transcript.py` to
  `.github/workflows/main_ci.yml` (mypy typed-island list), to
  `AGENT.md` Typed Island, and to `docs/MODULE_MAP.md`.

Historical phase-A gates on `rebuild/inline-terminal-0.3.7`:

- 1481/1481 non-E2E tests pass.
- 25/26 Dynamic E2E tests pass. The remaining failure is
  `tests/test_e2e.py::test_slash_planguard_tui_applies_selected_mode`
  and is exactly the phase B port that this plan defers.
- `python -m ruff check .` is clean.
- `python -m mypy pawnlogic/terminal_transcript.py` reports 0 issues.
- `python -m mypy pawnlogic/live_terminal.py` reports 0 issues.
- `tools/check_doc_structure.py` and `tools/check_release_consistency.py`
  pass.

### Phase B — interactive selectors (merged; publication pending)

The four interactive selectors that still owned their own PT
`Application` and were slated to be ported to
`controller.run_selector` are now all routed through the controller
when one is attached to the `CommandContext`:

1. `/model` — `cc_style_model_selector` in
   `core/commands/provider.py`. **Locally implemented** through
   `controller.run_selector` when a controller is attached.
2. `/planguard` — `_select_plan_guard_mode` in
   `core/commands/system.py`. **Locally implemented** through
   `controller.run_selector`; the E2E test
   `test_slash_planguard_tui_applies_selected_mode` is green. Notices
   stay inside the terminal transcript instead of bypassing the live
   renderer.
3. `/provider` — the live command creates a `ProviderTUI` state object
   whose container, focus target, and key bindings are mounted by
   `controller.run_selector` inside the persistent Application.
4. `/skills` — the live command creates a `SkillPackTUI` state object
   and mounts the same rich-modal contract inside the persistent
   Application.

Each port:

- Schedules the selector through `controller.run_selector`, so the
  main `Application` task identity is preserved across the round
  trip and `Application.exit()` is not called on the main app.
- Adds public-command integration tests that execute the real provider
  and skills factories and reject a second `Application`, a host
  `Application.exit()`, or a changed host task identity.
- Falls back to the original independent-`Application` path when
  no controller is attached, so readline-only sessions and tests
  keep working.

**Application identity.** All four live selectors now share the host
Prompt Toolkit `Application`. `/provider` and `/skills` retain their
standalone Application only for serial/readline operation when no live
controller is attached.

Merged implementation checklist (publication acceptance still pending):

- ✓ The E2E test listed above flipped back to green.
- ✓ This Evidence section is extended with the phase B commit chain.
- ✓ `CHANGELOG.md` has a 0.3.7 candidate section.
- ✓ `SECURITY.md` has a 0.3.7 supported-version row.
- ✓ `config/paths.py:VERSION` declares 0.3.7 as the unreleased candidate.
- ⏳ ADR 0010 is moved from **Proposed** to **Accepted** in
  `docs/adr/0010-inline-terminal-modal.md`.
- ✓ `AGENT.md` Current Release State records the paused 0.3.7 candidate.
- ✓ A real PTY smoke test by the human owner confirms host terminal
  scrollback, mouse selection, and copy-paste still work while the
  composer is up and while each of the four selectors is on screen.
- ✓ `/provider` and `/skills` use the embedded single-Application modal.
- ✓ Live completed lines reach host scrollback through
  `run_in_terminal`; partial lines flush once at close, and transient
  host-write failures retry without losing text. Retries are bounded with
  backoff and a circuit breaker, including the final partial-line handoff.
- ✓ Selector replacement and terminal shutdown resolve only the matching
  selector future, so stale `finally` blocks cannot close a newer modal and
  no command task remains suspended after terminal close.
- ✓ Local combined gates: 1,521 non-E2E tests and 29 Dynamic E2E tests,
  plus Ruff, relevant mypy, documentation/release/architecture checks,
  code-index refresh, and `git diff --check`.
- ✓ PR #124 merged the accepted implementation to `main` as `7a40374`;
  PR and post-merge CI passed Ruff, mypy, docs/language/release guards,
  Python 3.10/3.11/3.12 coverage, Fast Tests, and Dynamic E2E.

The owner-directed queue corrections in `ccfd5e8`, `feb280f`, and
`bc31894` amend the original frozen-queue non-goal: conditional queue
preview, gesture-based queue editing, Esc queue handoff, mid-Turn `/q`,
and clean shutdown are part of the 0.3.7 acceptance surface. This does
not authorize further queue or policy expansion.

0.3.7 remains an unreleased candidate after merge. It must not be tagged or
published until the owner explicitly resumes the paused release process.
