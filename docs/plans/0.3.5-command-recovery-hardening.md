# PawnLogic 0.3.5 Command Recovery Hardening Plan

> **Status:** Complete. v0.3.5 was tagged and published on 2026-08-30;
> implementation and release evidence is recorded below.

## Goal

Finish the command-recovery work merged after 0.3.4 so fuzzy slash-command
dispatch and interrupted-Turn recovery have one unambiguous, tested contract
before 0.3.5 is released.

## Scope

- Auto-dispatch a fuzzy slash command only when exactly one registered command
  matches. Ambiguous input must execute nothing and list its candidates.
- Keep Prompt Toolkit, readline, and direct dispatch on the same fuzzy-matching
  semantics and the live `core.commands.COMMANDS` registry.
- After a Turn interruption, Enter retries the preserved prompt once and an
  edited prompt replaces it without duplication, including edits beginning
  with `/`.
- During interrupted-input recovery, `/queue`, `/queue resume`, `/queue clear`,
  and `/abort` remain control commands instead of replacing the preserved Turn.
- Keep the English and Chinese recovery documentation and terminal hint in
  sync.
- Close the release-gate network-policy gap by rejecting `localhost` and every
  reserved `.localhost` subdomain before DNS resolution.

## Acceptance Gates

- Regression tests prove `/plg` reaches `/planguard`, while ambiguous examples
  such as `/qu` and `/md` execute no command and show all matching candidates.
- A terminal-level recovery test proves an edited `/...` prompt replaces the
  preserved queue head exactly once and remains retryable after another
  interruption; queue, clear, resume, and abort controls remain reachable.
- The recovery hint includes Enter, edit, `/queue resume`, and `/abort` paths.
- Resolver-independent tests prove the localhost namespace is classified as
  loopback and never reaches DNS.
- Relevant tests, E2E, ruff, typed-island mypy, documentation checks, release
  consistency, architecture budget, code-index freshness, and `git diff
  --check` pass.
- A release candidate later passes the Python 3.10/3.11/3.12 matrix, package
  build, fresh-install smoke, Dynamic E2E, and remote Actions before publish.

## Explicit Non-Goals

- No version bump, tag, PyPI upload, GitHub Release, or remote branch operation
  in the implementation step.
- No change to plan-guard enforcement semantics or Provider behavior.
- No cleanup or merge of stale local branches without separate authorization.

## Evidence

- Baseline: `main` at `1cdd12b` (PR #116 merged).
- Local implementation commits `206737a` and `b714ad4` were merged into local
  `main` by `dafc3e1`; release-preparation review fixes were committed as
  `7217eaf`. The combined history is published on `test/release-0.3.5`.
- RED evidence: ambiguous `/qu` executed `/quit`; an interrupted edit to
  `/replacement prompt` started no second Turn; the recovery hint omitted
  `/abort`. Release-preparation review additionally reproduced that pressing
  Enter after interrupting `/replacement prompt` a second time executed it as
  an unknown slash command instead of retrying the preserved Turn.
- GREEN evidence: 211 command/completion/CLI/session tests, 85 network-policy
  tests, and 17 terminal E2E tests pass. The previous `service.localhost` failure was
  reproduced with a pure policy test, then fixed by denying the normalized
  localhost namespace before invoking the resolver.
- The fast suite passes with 1373 tests (7 deselected); the complete non-E2E
  suite passes with 1380 tests. Both use an isolated `PAWNLOGIC_HOME`.
- Ruff, typed-island mypy (32 files), documentation structure, release
  consistency, architecture budget, repository language policy, code-index
  freshness, four CLI smoke paths, offline runtime evaluation, and `git diff
  --check` pass locally.
- Release candidate `af356d5` passes an isolated source-archive build and
  `twine check`; the 0.3.5 wheel reports matching runtime/package metadata,
  contains zero bundled `skills/` or `pawnlogic_security` entries, and passes
  `pawn --help` plus `python -m pawnlogic --help` from a fresh virtualenv.
- Remote CI run
  [33318954584](https://github.com/john0123412/PawnLogic/actions/runs/33318954584)
  passes Ruff, documentation guards, typed-island mypy, the complete Python
  3.10/3.11/3.12 non-E2E matrix, and Dynamic E2E. Final release commit IDs,
  the tag-triggered publish run, and release URLs remain pending.
