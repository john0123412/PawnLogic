# 0.3.10 — Terminal Hardening and Release

> **Status:** Published (`v0.3.10`, 2026-09-09, PyPI + GitHub Release,
> run [`34327728090`](https://github.com/john0123412/PawnLogic/actions/runs/34327728090)).
> The initial candidate failed owner terminal acceptance; the repair
> (#144/#145) merged and passed the full release gate. Two items stay
> open after the release: owner terminal acceptance on the published
> binary (Windows Terminal / WSL) and the tool-stage stall diagnosis
> (needs owner inputs). A post-release wire-tail fix is recorded under
> `[Unreleased]` in CHANGELOG.md.

## Scope

Terminal stability and release-gate hardening on top of 0.3.9:

1. Live-terminal single-owner display: completed output appears exactly
   once — in the native host scrollback — while the application viewport
   shows only undelivered lines (ADR 0010 discipline, no new scheduler).
2. Release-gate hardening: the pre-commit leak scanner cannot be bypassed
   by staging the scanner itself; toolchain pinned for the Rust gates.
3. Server-command override for packaged ratatui frontends
   (`PAWNLOGIC_TUI_SERVER`).
4. Session/workspace split: `session_<id>/` scratch directories live under
   `~/.pawnlogic/sessions/`; `workspace/` keeps only user-visible outcomes.
5. Ratatui usability repair: eliminate stream/result duplication, make
   backend failures visible, restore terminal state on every exit path, and
   provide a cursor-aware Unicode composer with paste and mouse scrolling.

## Collected implementation (evidence)

| Change | PR | Merged |
|--------|----|--------|
| ratatui server-command override | #138 | 603aa80 ancestor (3f26b88) |
| precommit leak-scan bypass fix + regression suite | #140 | 603aa80 |
| live-terminal display ownership, debounce, pre-wrap, sessions split | #141 | b8968ff |
| live spinner gating and terminal replay diagnostics | #144 | b0a0eb0 |
| ratatui usability repair | #145 | fffb1d1 (released) |

Review round 3 (final acceptance): fast suite 1,578 passed / Dynamic E2E
31/31, ruff + mypy clean, BAI glm-5.3-flash real-PTY probe on a 40-column
terminal shows a completed turn exactly once; delivery cursor commits
inside the `run_in_terminal` window (timing regression
`test_first_frame_after_host_write_excludes_delivered_text`).

## Release evidence

- Release branch validation (candidate head `de1bce2`): run
  [`34323190852`](https://github.com/john0123412/PawnLogic/actions/runs/34323190852)
  — Python 3.10/3.11/3.12 full matrix, Dynamic E2E, Rust gates, mypy,
  docs, all green.
- Tagged publish (merge commit `fffb1d1`): run
  [`34327728090`](https://github.com/john0123412/PawnLogic/actions/runs/34327728090)
  — 1,624 non-E2E, Dynamic E2E 31/31, cargo test 24/24, twine, PyPI
  Trusted Publishing + install smoke, GitHub Release with binary +
  sha256.
- Post-publication checks done: PyPI latest = `0.3.10` with matching
  wheel/sdist sha256; release binary sha256 matches the asset digest;
  README badges and "current public release" pointers updated in both
  languages; `.release-ready` marker retained for the tag.

## Post-release follow-ups

1. Wire-tail loss (shipped defect, fixed in `[Unreleased]`): answers
   ending in a `<`-led fragment lost the tail on every event-wire
   consumer; renderer flush now rides `content_delta`, both wire
   clients append only the missing tail from `result`, ratatui gained
   its first draw-level regression. Ship in the next patch release.
2. Owner terminal acceptance on the published binary (Windows
   Terminal / WSL: glyph width, selection, copy) — still outstanding
   from the original gate.
3. Tool-stage stall diagnosis needs the owner's inputs (tool name,
   confirmation dialog shown, composer responsiveness).

## Explicitly out of scope

- No 0.4.0 bump, scheduler rewrite, or wire-protocol replacement.
- No claim of zhipu direct-route acceptance (still blocked on account
  balance; bai route is the verified path).

## Acceptance criteria

All gates green on the candidate SHA; release metadata consistent;
PyPI hashes and GitHub assets verified after publish. Gate status:
CI/e2e/publish gates green and verified post-publish; owner terminal
acceptance on the release binary remains open (see follow-ups).
