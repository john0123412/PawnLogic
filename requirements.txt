# PawnLogic 1.1 — Core Dependencies
# Install: pip install -r requirements.txt

# Required
loguru>=0.7.0
python-dotenv>=1.0.0
nest_asyncio>=1.6.0        # 允许嵌套事件循环（Jupyter/Scrapling 共存）

# P2: CLI UX — Terminal experience upgrade
prompt_toolkit>=3.0.0    # Fish-style completion, PromptSession, multiline input
rich>=13.0.0             # Markdown rendering, syntax highlighting, panels

# Optional (enhanced features)
# pandoc  — HTML→Markdown conversion (apt install pandoc)
# ROPgadget — ROP chain search (pip3 install ROPgadget)
# ropper  — Alternative ROP search (pip3 install ropper)
# one_gadget — execve gadget search (gem install one_gadget)
# pwntools — CTF exploit development (pip3 install pwntools, used inside run_code sandbox)
# docker  — P3: Docker containerization (pip install docker + Docker CE runtime)
# mcp     — P5: Scrapling browser toolkit (pip install mcp + Node.js >= 18 for npx)
