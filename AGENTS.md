# PawnLogic Codex Entry

This repository uses `AGENT.md` as the single source of truth for agent maintenance rules.

Before planning, editing, running tests, or changing configuration, read `AGENT.md` first.

Do not duplicate, override, or expand project rules here. If a repository rule needs to change, update `AGENT.md`.
